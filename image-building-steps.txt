1. docker login
2. docker buildx create --use
3. docker buildx build \
  --platform linux/amd64,linux/arm64 \
  -t amandeeptruinc/order-manage-app-backend:v1.0.5 \
  -t amandeeptruinc/order-manage-app-backend:latest \
  --push .
4. docker-compose up --pull always --force-recreate // to run app on local